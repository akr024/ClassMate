import express, {Router} from 'express'
import bcrypt from 'bcrypt'
import {z} from 'zod';
import { UserModel } from '../models/userModel.js';

const apiRouter = express.Router();

apiRouter.use('/users/signup', )



export default apiRouter;